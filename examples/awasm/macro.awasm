jro main

!include <hello_world.awasm>

main:
        !def foo(val, bar)
        blo $val
        $bar
        !end

        call func
        !foo 1, prn
        !foo 2, prn
        !foo 3, prn

        blo 63
        prn
